{% extends "dashboard/dashboard.html.twig" %}

{% block content %}
	<div class="dashboard_content">
		<header>
			<h1>Edit Presentation</h1>
		</header>
		<div class="add_edit_form dashboard_content_main_box">
			<p>Edit a presentation.</p>
			{{ form(form) }}
			{% if is_granted('ROLE_ADMIN') %}
				<div class="delete_actions">
					<a href="{{ path('delete_presentation', {uuid:uuid_encode(presentation.uuid)}) }}" class="button danger">Delete presentation</a>
				</div>
			{% endif %}
		</div>
	</div>
{% endblock %}