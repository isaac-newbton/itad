{% extends "dashboard/dashboard.html.twig" %}

{% block content %}
	<div class="dashboard_content">
		<header>
			<h1>Publications</h1>
		</header>
		<div class="publications_list">
			{% if publications is not empty %}
				{% for publication in publications %}
					<div class="publication_container">
						<article>
							<header>
								<span class="meta">{{ publication.dt|date('m/d/Y') }}</span>
								<h2>{{ publication.name }}</h2>
							</header>
							<div class="excerpt">
								<em>{{ publication.author }}</em>
							</div>
						</article>
					</div>
				{% endfor %}
			{% else %}
				<p>No publications are posted yet.</p>
			{% endif %}
			{% if is_granted('ROLE_ADMIN') %}
				<p><a href="{{ path('add_publication') }}" class="button">Add Publication</a></p>
			{% endif %}
		</div>
	</div>
{% endblock %}