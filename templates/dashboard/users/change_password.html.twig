{% extends "dashboard/dashboard.html.twig" %}

{% block content %}
	<div class="dashboard_content">
		<header>
			<h1>Edit Password for {{ app.user.email }}</h1>
		</header>
		<div class="add_edit_form">
			<a href="{{ path('user_profile') }}">Back to profile</a>
			{{ form(form) }}
		</div>
	</div>
{% endblock %}